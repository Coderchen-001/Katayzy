# Normalize line endings: store all text files as LF in the repository.
* text=auto

# Windows-native scripts must stay CRLF on checkout so cmd.exe runs them safely.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
